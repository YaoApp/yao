# Package `api`

> **[已移除]** Direct 模式的 HTTP 注册 API（`/tai-nodes/register`、`/tai-nodes/heartbeat`、`/tai-nodes/register/:tai_id`）已被移除。
> Tai 节点现在统一通过 tunnel（WebSocket gRPC）模式向 Yao 注册。
>
> 如需了解 tunnel 模式的注册流程，请参阅 [registry.md](registry.md) 和 [tunnel.md](tunnel.md)。
