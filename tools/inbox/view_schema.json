{
  "name": "inbox_view",
  "description": "Mark a task as viewed in inbox (clears unread)",
  "process": "tools.inbox_view",
  "inputSchema": {
    "type": "object",
    "properties": {
      "chat_id": { "type": "string", "description": "Chat ID of the task to mark as viewed" }
    },
    "required": ["chat_id"]
  },
  "x-process-args": ["$args.chat_id"]
}
