{
  "name": "task_run",
  "description": "Run or resume a task. Sends a message to start execution.",
  "parameters": {
    "type": "object",
    "properties": {
      "chat_id": {
        "type": "string",
        "description": "The task chat ID to run"
      },
      "message": {
        "type": "string",
        "description": "The message to send to the task"
      }
    },
    "required": ["chat_id", "message"]
  }
}
