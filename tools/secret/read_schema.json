{
  "name": "secret_read",
  "description": "Read a secret value by name. Returns the decrypted value for use in scripts. Never log or print the returned value.",
  "process": "tools.secret_read",
  "inputSchema": {
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "description": "The secret key name (e.g. GITHUB_TOKEN, AWS_SECRET_KEY)"
      }
    },
    "required": ["name"]
  },
  "x-process-args": ["$args.name"]
}
